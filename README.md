```
All functions will only work with if the array is empty or all its elements are objects with the fields "name" and "kol"
```

```

addorks()-this is a function that adds items to the basket, but if this item is already in the basket, it increases its number by 1

```

```
deletkors()-this function will delete the entered item from the trash if it is there. It accepts an array as input
```

```
vivod()-this function outputs the trash. It accepts an array as input
```
